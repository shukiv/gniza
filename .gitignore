/bin/
*.test
coverage.out
.env
*.pem
*.key
.htpasswd
/.tmp/
/pki/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Node packages for the stylesheet build (make css); the compiled
# stylesheet is committed, so a Go build never needs them.
/internal/webui/ui/node_modules/
